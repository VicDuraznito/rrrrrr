<div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
  <div class="bg-white overflow-hidden shadow-xl sm:rounded-lg mt-12">

    <table class="table-fixed w-full">
      <thead>
        <tr class="bg-green-500 text-white">
          <th class="w-20 py-4 ...">ID</th>
          <th class="w-1/4 py-4 ...">NAME</th>
          <th class="w-1/2 py-4 ...">LASTNAME</th>
        
        </tr>
      </thead>
      <tbody>
        {{#each users}}
        <tr>
          <td class="py-3 px-6">{{id}}</td>
          <td class="p-3">{{first_name}}</td>
          <td class="p-3 text-center">{{Last_name}}</td>
          <td class="p-3 flex">
           
              <button class="bg-red-500 text-white px-3 py-1 rounded-sm" type="submit">
                <i class="fas fa-trash"></i></button>
            </form>

          </td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</div>